export enum RequestStatus {
  IDLE = "idle",
  PENDING = "pending",
  ERROR = "error",
  SUCCESS = "success",
}

export enum UserState {
  LOGGED_IN = "LOGGED_IN",
  LOADING = "LOADING",
  GUEST = "GUEST",
}
